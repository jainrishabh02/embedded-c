#include <stdio.h>
#include <stdint.h>
int32_t multiplyByNine(int32_t num) {
    return (num << 3) + num;
}
int main(void) {
    int32_t number = 7;
    int32_t result = multiplyByNine(number);
    printf("Result of %d * 9 = %d\n", number, result);
    return 0;
}
